1. Run [pip install flask] in terminal
2. Create "CookSense AI/.env"
3. Enter the following: [API_KEY = "Insert_API_key"]
4. If google.genai is NOT installed, run [pip install google-genai python-dotenv Flask]
5. To enable picture compression, run [pip install Pillow]
